// 3. შექმენით ობიექტი და ციკლის საშუალებით დაბეჭდეთ key-იები;
let names = ['tornike', 'nata', 'lile', 'lasha'];
for (let item in names) {
    console.log (item);
}

// 4. შექმენით ობიექტი და ციკლის საშუალებით დაბეჭდეთ მნიშვნელობები

let x = ['tornike', 'nata', 'lile', 'lasha'];
 for(let item of x) {
     console.log (item);
 }